class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        s=set()
        for curr in nums:
            if curr in s:
                return True
            else:
             s.add(curr)
        return False

        
